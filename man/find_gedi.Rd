% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/search.R
\name{find_gedi}
\alias{find_gedi}
\title{Find GEDI granules for a spatial area}
\usage{
find_gedi(
  bbox,
  product = c("L2A", "L2B", "L4A", "L1B"),
  date_start = NULL,
  date_end = NULL
)
}
\arguments{
\item{bbox}{An \code{sl_bbox} object created by \code{\link[=sl_bbox]{sl_bbox()}}, or a numeric vector
\code{c(xmin, ymin, xmax, ymax)}.}

\item{product}{Character. GEDI product level: \code{"L2A"}, \code{"L2B"}, \code{"L4A"}, or
\code{"L1B"}.}

\item{date_start}{Character or POSIXct. Start of date range (default:
\code{"2019-03-25"}, the start of GEDI operations).}

\item{date_end}{Character or POSIXct. End of date range (default: today).}
}
\value{
A data frame with columns:
\describe{
\item{id}{CMR granule identifier.}
\item{time_start}{POSIXct start time of the granule.}
\item{time_end}{POSIXct end time of the granule.}
\item{url}{HTTPS data URL — pass to \code{\link[=grab_gedi]{grab_gedi()}}.}
\item{geometry}{\code{wk_wkt} polygon of the granule swath footprint.}
}
}
\description{
Searches NASA's Common Metadata Repository (CMR) for GEDI granules that
overlap a bounding box and optional date range. Returns granule URLs that
can be passed directly to \code{\link[=grab_gedi]{grab_gedi()}}.
}
\details{
No authentication is needed for the search itself; a token is only
required when reading data via \code{\link[=grab_gedi]{grab_gedi()}}.

The CMR search filters by bounding box on the server side. For finer
spatial filtering (e.g. against an irregular polygon), filter the returned
\code{geometry} column with \code{\link[wk:wk_intersects]{wk::wk_intersects()}} or similar.
}
\seealso{
\code{\link[=grab_gedi]{grab_gedi()}} to read data from the returned URLs.
}
