% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/icesat2.R
\name{grab_icesat2}
\alias{grab_icesat2}
\title{Read ICESat-2 data with spatial subsetting}
\usage{
grab_icesat2(
  url,
  product = c("ATL08", "ATL03", "ATL06"),
  bbox,
  columns = NULL,
  tracks = NULL,
  token = NULL
)
}
\arguments{
\item{url}{Character. URL of the ICESat-2 HDF5 file (HTTPS or S3).}

\item{product}{Character. ICESat-2 product: \code{"ATL08"}, \code{"ATL03"}, or
\code{"ATL06"}.}

\item{bbox}{An \code{sl_bbox} object created by \code{\link[=sl_bbox]{sl_bbox()}}, or a numeric vector
\code{c(xmin, ymin, xmax, ymax)}.}

\item{columns}{Character vector of dataset paths to read (relative to the
ground track group, e.g., \code{"land_segments/canopy/h_canopy"}). If \code{NULL},
reads the standard columns for the specified product.}

\item{tracks}{Character vector of ground track names (e.g., \code{"gt1l"}). If
\code{NULL}, reads all 6 tracks.}

\item{token}{Bearer token for NASA Earthdata authentication.}
}
\value{
A data frame with one row per element (segment, photon, etc.).
Columns include the requested datasets plus a \code{track} identifier and a
\code{geometry} column (\code{wk_xy}).
}
\description{
Reads ICESat-2 satellite lidar data directly from a remote HDF5 file,
fetching only the data that falls within the specified bounding box.
}
\details{
\subsection{Products}{
\itemize{
\item \strong{ATL08}: Land/vegetation segments at 100m resolution. Best for canopy
height and terrain analysis.
\item \strong{ATL06}: Land ice elevation at ~40m resolution.
\item \strong{ATL03}: Individual photon heights. Very large datasets (millions of
rows per track). Use a small bounding box.
}
}
}
