% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/auth.R
\name{sl_earthdata_token}
\alias{sl_earthdata_token}
\title{Get a NASA Earthdata bearer token}
\usage{
sl_earthdata_token(token = NULL)
}
\arguments{
\item{token}{Optional token string. If provided, returned as-is.}
}
\value{
A character string with the bearer token, or \code{NULL} if no
credentials are found.
}
\description{
Retrieves a bearer token for authenticating with NASA Earthdata HTTPS
endpoints. Credentials are resolved in this order:
}
\details{
\enumerate{
\item The \code{token} argument (pass-through)
\item The \code{EARTHDATA_TOKEN} environment variable (a pre-existing bearer token)
\item \code{EARTHDATA_USERNAME} + \code{EARTHDATA_PASSWORD} environment variables
\item A \verb{~/.netrc} entry for \code{urs.earthdata.nasa.gov}
\item Interactive prompt (if running interactively)
}

For options 3-5, the username/password are exchanged for a bearer token
via the Earthdata Login token API. The token is cached for the session.
}
