% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{sl_grab}
\alias{sl_grab}
\alias{sl_grab.sl_gedi_search}
\alias{sl_grab.sl_icesat2_search}
\alias{sl_grab.character}
\title{Grab satellite lidar data}
\usage{
sl_grab(x, bbox, ...)

\method{sl_grab}{sl_gedi_search}(x, bbox, columns = NULL, beams = NULL, ...)

\method{sl_grab}{sl_icesat2_search}(x, bbox, columns = NULL, tracks = NULL, ...)

\method{sl_grab}{character}(x, bbox, ..., product = NULL)
}
\arguments{
\item{x}{An \code{sl_gedi_search}, \code{sl_icesat2_search}, or character vector of
HDF5 URLs.}

\item{bbox}{An \code{sl_bbox} or numeric \code{c(xmin, ymin, xmax, ymax)}. Required.}

\item{...}{Additional arguments passed to the underlying reader
(\code{\link[=grab_gedi]{grab_gedi()}} or \code{\link[=grab_icesat2]{grab_icesat2()}}).}

\item{product}{Character. Product level (e.g., \code{"L2A"}, \code{"ATL08"}).
Required when \code{x} is a character vector and the product cannot be
inferred from the file name.}
}
\value{
A data frame with one row per footprint/segment.
}
\description{
\code{sl_grab()} is an S3 generic that reads GEDI or ICESat-2 data from remote
HDF5 files. It dispatches on the type of its first argument:
}
\details{
\itemize{
\item An \code{sl_gedi_search} or \code{sl_icesat2_search} object (from \code{\link[=find_gedi]{find_gedi()}} /
\code{\link[=find_icesat2]{find_icesat2()}}) — reads all granules in the search result, combining
rows into a single data frame.
\item A character vector of URLs — auto-detects the sensor and product from the
file name, or uses the explicit \code{product} argument.
}
}
\seealso{
\code{\link[=find_gedi]{find_gedi()}}, \code{\link[=find_icesat2]{find_icesat2()}}, \code{\link[=grab_gedi]{grab_gedi()}}, \code{\link[=grab_icesat2]{grab_icesat2()}}
}
