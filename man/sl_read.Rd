% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{sl_read}
\alias{sl_read}
\alias{sl_read.sl_gedi_search}
\alias{sl_read.sl_icesat2_search}
\alias{sl_read.character}
\title{Read satellite lidar data}
\usage{
sl_read(x, bbox, ...)

\method{sl_read}{sl_gedi_search}(x, bbox = NULL, columns = NULL, convert_time = TRUE, ...)

\method{sl_read}{sl_icesat2_search}(x, bbox = NULL, columns = NULL, convert_time = TRUE, ...)

\method{sl_read}{character}(x, bbox, ..., product = NULL)
}
\arguments{
\item{x}{An \code{sl_gedi_search}, \code{sl_icesat2_search}, or character vector of
HDF5 URLs.}

\item{bbox}{An \code{sl_bbox} or numeric \code{c(xmin, ymin, xmax, ymax)}. Required
when \code{x} is a character vector. Optional when \code{x} is a search result: it
defaults to the bbox the search was performed with. If supplied, it
must be fully contained within the search bbox; supplying a wider bbox
is an error to avoid silently missing data outside the original search.}

\item{...}{Reserved for method-specific arguments and forwarding.}

\item{columns}{Character vector of column names to read (short names from
\code{\link[=sl_columns]{sl_columns()}}). Latitude and longitude are always included
automatically. \code{NULL} (default) reads the curated default column set
for the product (see \code{\link[=sl_columns]{sl_columns()}} with \code{set = "default"}). Pass
\code{names(sl_columns(product))} to read all available columns.}

\item{convert_time}{Logical. If \code{TRUE} (default), the raw \code{delta_time}
column (seconds since the GEDI / ICESat-2 reference epoch of
2018-01-01 00:00:00 UTC) is converted to a POSIXct column named
\code{time}. Set to \code{FALSE} to keep \code{delta_time} as the raw numeric
seconds-since-epoch value, e.g. if you need to compare against the
file-level epoch exactly, want to avoid the POSIXct conversion
overhead on very large photon-level reads, or want to preserve the
original HDF5 column name.}

\item{product}{Character. Product level (e.g., \code{"L2A"}, \code{"ATL08"}).
Required when \code{x} is a character vector and the product cannot be
inferred from the file name.}
}
\value{
A data frame with one row per footprint (GEDI) or
segment/photon (ICESat-2). Columns depend on the product and the
\code{columns} argument. Fill-value sentinels (-9999, 3.4e38, etc.) are
automatically replaced with \code{NA}.
}
\description{
\code{sl_read()} is an S3 generic that reads GEDI or ICESat-2 data from remote
HDF5 files using HTTP range requests. Only the chunks intersecting the
bounding box are fetched; no full-file download. It dispatches on the type
of its first argument:
}
\details{
\itemize{
\item An \code{sl_gedi_search} or \code{sl_icesat2_search} object (from \code{\link[=sl_search]{sl_search()}}):
reads all granules in the search result, combining rows into a single
data frame. The search bbox is used by default; an explicit \code{bbox} may
be supplied to subset further but must be contained within it.
\item A character vector of URLs: auto-detects the sensor and product from the
file name, or uses the explicit \code{product} argument.
}

All beams (GEDI) or ground tracks (ICESat-2) are always read; the returned
data frame includes a \code{beam} (GEDI) or \code{track} (ICESat-2) identifier
column for post-hoc filtering with \code{\link[dplyr:filter]{dplyr::filter()}} or base subsetting.
\subsection{Default columns}{

When \code{columns = NULL}, a curated default set is returned for each
product. Use \code{sl_columns(product, set = "default")} to see which
columns are included. The defaults are designed to cover the primary
science variables, key quality flags, and basic context without
surprises. Use \code{sl_columns(product, set = "all")} to discover
everything available.
}

\subsection{Product-specific notes}{

\strong{GEDI L1B}: The default set includes \code{rxwaveform}, which is a
\strong{list column} (one numeric vector per shot containing the received
waveform). Use \code{\link[=sl_extract_waveforms]{sl_extract_waveforms()}} to expand this into a
long-form data frame with per-sample elevations. The transmitted
waveform (\code{txwaveform}) is available via explicit request but not
included in defaults. L1B reads are slower than other products
because waveform data requires targeted chunk reads into the pool
dataset.

\strong{GEDI L2A}: The \code{rh} column is a 2D dataset [N, 101] that
expands into 101 columns (\code{rh0} through \code{rh100}), representing
relative height percentiles in metres. This is included in the
default set.

\strong{GEDI L2B}: The default set includes \code{cover_z}, \code{pai_z}, and
\code{pavd_z}, which are 2D datasets [N, 30] representing canopy
vertical profiles at 5 m height bins. Each expands to 30 columns
(e.g. \code{cover_z0} through \code{cover_z29}), adding 90 columns to the
output. \code{pgap_theta_z} is a variable-length list column (similar to
L1B waveforms) and is not included in defaults; request it
explicitly when needed.

\strong{GEDI L2B \code{rh100}}: Stored in centimetres in the HDF5 file;
automatically converted to metres for consistency with L2A.

\strong{GEDI L4A}: The \code{agbd} column is above-ground biomass density in
Mg/ha. Prediction intervals (\code{agbd_pi_lower}, \code{agbd_pi_upper}) and
standard error (\code{agbd_se}) are included in defaults.

\strong{GEDI L4C}: Waveform Structural Complexity Index. The \code{wsci}
column is the headline metric, with prediction intervals
(\code{wsci_pi_lower}, \code{wsci_pi_upper}) and decomposed XY/Z components
(\code{wsci_xy}, \code{wsci_z}) in defaults. \code{worldcover_class} provides the
ESA WorldCover land-cover class.

\strong{ICESat-2 ATL03}: Photon-level data. A single granule can contain
millions of photons. The reader uses ATL03's segment-level spatial
index (\code{geolocation/reference_photon_lat} etc.) to filter at segment
rate before reading photon-level columns, so spatial subsets stay
fast even on large bboxes. \code{signal_conf_ph} is a 2D column
[N, 5] (5 surface types: land, ocean, sea ice, land ice, inland
water) that expands to 5 columns.

\strong{ICESat-2 ATL06}: Land ice elevation segments. The default set
includes fit statistics (\code{n_fit_photons}, \code{h_robust_sprd}, \code{snr})
and reference DEM height (\code{dem_h}). Tidal and geophysical
corrections are available via \code{sl_columns("ATL06")} but not in
defaults.

\strong{ICESat-2 ATL07}: Sea-ice height segments. Defaults include
segment height + confidence + quality, photon rate, AMSR2 ice
concentration, and atmospheric flags. Geolocation parameters
(\verb{solar_*}, \code{sigma_h}) and finer geophysical corrections live
under \verb{sea_ice_segments/\{geolocation,geophysical,stats\}/} and are
available via \code{sl_columns("ATL07", set = "all")}.

\strong{ICESat-2 ATL08}: The default set includes \code{canopy_h_metrics}, a
2D dataset [N, 18] of canopy height percentiles (P10 through P95)
that expands to 18 columns. Terrain slope, photon counts, and land
cover are also included. \verb{*_abs} (absolute height) variants and
the secondary canopy / terrain metrics are in the registry but not
in defaults.

\strong{ICESat-2 ATL10}: Sea-ice freeboard. Defaults include
\code{beam_fb_height} (per-beam freeboard) plus quality, confidence,
and the underlying ATL07 height-segment context.

\strong{ICESat-2 ATL13}: Inland water surface heights. Defaults include
water-surface height + standard deviation, significant wave height,
water depth, segment provenance (\verb{inland_water_body_*}), and
quality flags. Geometry uses the segment centroid (\code{segment_lat},
\code{segment_lon}).

\strong{ICESat-2 ATL24}: Near-shore bathymetry, photon-level. Defaults
include orthometric / ellipsoidal / surface heights, photon class
and confidence, and the THU / TVU positional uncertainty pair.
Geometry uses photon coordinates (\code{lat_ph}, \code{lon_ph}).
}
}
\seealso{
\code{\link[=sl_search]{sl_search()}}, \code{\link[=sl_columns]{sl_columns()}}, \code{\link[=sl_extract_waveforms]{sl_extract_waveforms()}}
}
