% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/search.R
\name{sl_search}
\alias{sl_search}
\title{Search NASA CMR for GEDI or ICESat-2 granules}
\usage{
sl_search(
  bbox,
  product = c("L2A", "L2B", "L4A", "L4C", "L1B", "ATL08", "ATL03", "ATL06"),
  date_start = NULL,
  date_end = NULL
)
}
\arguments{
\item{bbox}{An \code{sl_bbox} object created by \code{\link[=sl_bbox]{sl_bbox()}}, or a numeric vector
\code{c(xmin, ymin, xmax, ymax)}.}

\item{product}{Character. One of:
\itemize{
\item GEDI: \code{"L1B"}, \code{"L2A"}, \code{"L2B"}, \code{"L4A"}
\item ICESat-2: \code{"ATL03"}, \code{"ATL06"}, \code{"ATL08"}
}}

\item{date_start}{Character or POSIXct. Start of date range. Defaults to
the start of the relevant mission (2019-03-25 for GEDI, 2018-10-14 for
ICESat-2).}

\item{date_end}{Character or POSIXct. End of date range (default: today).}
}
\value{
A classed data frame (\code{sl_gedi_search} or \code{sl_icesat2_search})
with columns:
\describe{
\item{id}{CMR granule identifier.}
\item{time_start}{POSIXct start time of the granule.}
\item{time_end}{POSIXct end time of the granule.}
\item{url}{HTTPS data URL.}
\item{geometry}{\code{wk_wkt} polygon of the granule swath footprint.}
}
The returned object carries \code{bbox} and \code{product} as attributes so
\code{\link[=sl_read]{sl_read()}} can dispatch without re-specifying them.
}
\description{
Searches NASA's Common Metadata Repository (CMR) for satellite lidar
granules that overlap a bounding box and optional date range. The sensor
(GEDI or ICESat-2) is determined automatically from \code{product}, since the
valid product strings do not overlap.
}
\details{
No authentication is needed for the search itself; Earthdata credentials
are only required when reading data via \code{\link[=sl_read]{sl_read()}}.

The CMR search filters by bounding box on the server side. For finer
spatial filtering (e.g. against an irregular polygon), filter the returned
\code{geometry} column with your favourite spatial package.
}
\seealso{
\code{\link[=sl_read]{sl_read()}} to read data from the returned granules.
}
